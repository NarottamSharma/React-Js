import React from 'react'
import { useForm } from 'react-hook-form'

const Form = () => {
  const {register,handleSubmit,formState:{errors,isSubmit}} = useForm()
  
  return (
    <div>
      
    </div>
  )
}

export default Form