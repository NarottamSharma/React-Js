import React from 'react'

const Cart = ({name,age,city}) => {
  return (
    <div>
      <h1>My Name is {name}</h1>
      <p>My age is {age} and i live in {city}</p>
    </div>
  )
}

export default Cart